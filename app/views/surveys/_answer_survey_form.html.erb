<%# In this file, we should render the _answer_question_form.html.erb
partial for each question in the survey, allowing the user to answer them in turn
- perhaps include a button to start each question - this can help to measure
the time taken to answer each one? %>
<div class="row" style="background-image: url(<%= asset_path 'background_6.jpg' %>);min-height: calc(100% - 88px);">
  <div class="col">
    <% questions = @survey.questions %>
    <% questions.each do |question| %>
      <%= render 'answer_question_form', question_answer: QuestionAnswer.new, question: question %>
    <% end %>
    <%= button_to 'Like', :params => {like: true, survey: @survey} %>
    <%= button_to 'Dislike', :params => {like: false, survey: @survey} %>
    <div class="actions">
      <%= link_to 'Submit Answers', survey_path(@survey) + '?answered=true' %>
    </div>
  </div>
</div>
